////////////////////////for of ////////////////////////////////

const arr = [1,2,3,4,5]
for (const num of arr) {
    // console.log(num);
    
}

const greetings = "hello word"
 for (const greet of greetings) {
    // console.log(`each char is ${greet}`);
    
 }

 /////////////////////////////map/////////////////////////////////


 const map = new Map();  // Create a new Map instance

 // Set key-value pairs in the map
 map.set('rajkot', 'gujarat');
 map.set('ahmedabad', 'gujarat');
 map.set('surat', 'gujarat');
 
 // Output the map to the console
 console.log(map);
 